"use client";
import React from 'react';

// Legacy SPA entrypoint (now migrated to Next.js App Router).
// Keep a minimal, inert component to avoid runtime import issues during migration.
export default function App() {
  return null;
}
